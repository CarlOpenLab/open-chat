{
  "name": "@cc-hearts/open-chat",
  "version": "1.0.0",
  "description": "Open Chat local AI CLI workspace",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/cc-hearts/open-chat.git",
    "directory": "tools/open-chat"
  },
  "bin": {
    "open-chat": "./bin/open-chat.mjs"
  },
  "files": [
    "bin",
    "dist",
    "website-dist",
    "README.md"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "esbuild src/cli.ts --bundle --platform=node --format=cjs --target=node22 --outfile=dist/cli.cjs --banner:js=\"#!/usr/bin/env node\" --sourcemap",
    "dev": "tsx src/cli.ts",
    "start": "node bin/open-chat.mjs",
    "prepack": "pnpm --dir ../.. run build:package"
  },
  "devDependencies": {
    "@types/express": "catalog:",
    "@types/node": "catalog:",
    "esbuild": "catalog:",
    "express": "catalog:",
    "server": "workspace:*",
    "tsx": "^4.21.0",
    "typescript": "catalog:"
  }
}
